#include "iostream"
using namespace std;

int main() {
    float a, b, P, S;
    cin >> a >> b;

    P = 2 * (a + b);
    S = a * b;

    cout << P << endl;
    cout << S << endl;
    system("pause");
    return 0;
}